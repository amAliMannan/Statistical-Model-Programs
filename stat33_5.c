#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <string.h>

int main() {
    srand(time(NULL));
    int success = 0;
    int all = 0;

    char characters = "MOTHER";









    
    printf("\nsuccesses: %d\n attempts: %d\nprobabiltiy: %f\n", success, all, success * 1.0 / all);

    return 0;
}